// app-wide configuration (non application changeable settings)
module.exports = {
  // used package manager yarn or npm
  // Note! NPM with Windows not working at the moment because of a bug in NPM
  PACKAGE_MANAGER: 'yarn',
  // Enable logging, if enabled all terminal responses are visible in the console (useful for debugging)
  LOGGING: true,
};
